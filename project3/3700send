#!/usr/bin/env python
import sys
from socket import socket, AF_INET, SOCK_DGRAM

if __name__=='__main__':

    recv_addr = ('127.0.0.1', 5005)
    sock = socket(AF_INET, SOCK_DGRAM)

    data = sys.stdin.read()
    print "sending:", data

    sock.sendto(data, recv_addr)
